import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';


ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
/*Esta é a estrutura básica de uma aplicação React
Ele é responsável por processar as informações que está no projeto e apresentar o resultado(renderização.)*/